# 仓库 Agent 协作约束

## 语言规范

- 请使用中文回复、写文档、写代码注释，专业词汇使用英文。

## 代码注释规则（长期生效）

- 所有新增或修改的核心逻辑，必须补充简洁注释，优先解释“为什么这样做”。
- 注释使用中文，专业词汇使用英文；避免逐行翻译式注释。
- 若发现历史代码可读性不足，应在相关任务中顺带补齐必要注释。

## 设计对齐约束（长期生效）

- 本项目的实现必须主动对齐 `docs/` 目录下的设计文档；其中 `docs/AGENTS.md` 是设计文档索引和总体设计心智入口。
- 开始新增能力、修改核心流程、调整架构边界、变更协议或引入新依赖前，必须先阅读并遵守 `docs/AGENTS.md`，再按需阅读它链接的专题文档。
- 如果实现需求与现有设计文档冲突，必须先明确冲突点和可选取舍；凡是涉及修改设计文档、改变既有设计边界或调整核心方案的，必须先与用户确认，确认后再修改设计文档或代码。
- 不允许让实现绕过已确认的核心边界，包括但不限于：`Coordinator Core` 作为唯一状态机和策略校验者、`workflow` 私有状态不可被外层读写、agent 只能通过受控 surface 和 tools 行动、复杂信息优先写入 artifact。
- 每次使用 subagent review 时，必须把“是否符合 `docs/` 下总体设计心智和边界约束”作为必查项目，并在 review 请求中明确要求检查是否过度设计、是否偏离已有协议、是否污染分层边界。
- 后续通过提示词触发 OpenSpec 流程时，也必须先对齐 `docs/AGENTS.md` 与相关专题文档；OpenSpec change 不应成为绕开总体设计约束的平行规范。

## 提交协作规则（长期生效）

- Agent 可在任务推进过程中自动按需执行 `git add` 与 `git commit`，无需逐次向用户确认。
- 提交应以“一个相对完整、可验证、可回退的改动单元”为边界，避免每处理一点就提交一次。
- 当多个细碎修改属于同一轮目标、且继续累积不会显著增加回滚或定位风险时，优先合并为一次提交，不做过于频繁的碎片化提交。
- `git add` 范围可根据任务需要选择部分文件或全部文件，但应确保提交内容与当前目标一致，避免裹入无关改动。
- 提交信息必须使用中文，并包含清晰且相对详细的变更说明（建议包含背景、主要改动、验证结果）。
- 每个 OpenSpec change 完成并通过 review 后，必须先归档该 change，再对当前代码改动执行一次完整提交。
- 这样做的目标是让后续 change 始终在干净基线上推进，避免多个 change 的代码混淆在同一提交或同一未归档状态中。

## 本地运行与 build 约束

- `packages/cli` 和 `apps/api` 通过 workspace package 依赖加载 `@coordinator/core` 时，会走 `packages/core/package.json` 的 `exports`，实际读取 `packages/core/dist`，不是直接读取 `packages/core/src`。
- 因此修改 `packages/core/src` 后，如果要在 `pnpm cli ...`、`pnpm dev:api`、daemon tick 或本地 smoke 流程中验证效果，必须先执行 `pnpm --filter @coordinator/core build`。
- build 后，已启动的 `pnpm dev:api` 进程仍持有旧代码；需要重启 API 服务后，新逻辑才会在 Web/API/daemon 场景生效。
- 若只运行直接 import `src` 的单测，可能看起来已经通过，但本地服务和 CLI 仍可能使用旧 `dist`；涉及运行时行为修复时必须同时确认 build 产物已更新。
